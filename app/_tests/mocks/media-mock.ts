/**
 * Media Mock for Jest Tests
 *
 * Mocks media imports (mp4, webm, wav, mp3, etc.) to return
 * a simple mock path that can be used in tests.
 */

const mediaMock = 'mocked-media-file.mp4';

export default mediaMock;
