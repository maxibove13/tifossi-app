export interface Tag {
  id: string; // e.g., "tag_kids"
  name: string; // e.g., "Kids"
  slug: string; // e.g., "kids"
}

const utilityExport = {
  name: 'TagType',
  version: '1.0.0',
};

export default utilityExport;
