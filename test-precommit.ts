// This is a test file to check the pre-commit hook
const _unusedVar = 'This should trigger the linter';

function testFunction(): void {
  console.log('Hello world');
}

export default testFunction;
// test change
